# Server configuration
PORT=4000
NODE_ENV=development

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Origin Protocol ledger URL (see Python/tools/full_node_service.py)
ORIGIN_LEDGER_URL=http://127.0.0.1:9050
ORIGIN_PLATFORM_ID=origin-social

# Storage — local filesystem by default; swap for S3 in production
STORAGE_DRIVER=local
STORAGE_LOCAL_DIR=./uploads

# (Optional) S3-compatible storage
# STORAGE_DRIVER=s3
# S3_BUCKET=origin-social-videos
# S3_REGION=us-east-1
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
